% Sample script to demonstrate execution of function [z, x] = calculateDeformation(fixturePos, drillPos)
fixturePos = [400 800 10]; % Initialize fixturePos here
drillPos = [505.283 487.164 10]; % Initialize drillPos here
[z, x] = calculateDeformation(fixturePos, drillPos);
